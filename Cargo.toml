[package]
name = "camaps-fx"
version = "0.1.0"
edition = "2021"
description = "Cambridge hybrid closed-loop (CamAPS FX) style controller core with a Kani formal verification suite"
license = "MIT OR Apache-2.0"

[lib]
name = "camaps_fx"

[dev-dependencies]
proptest = "1"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(kani)'] }