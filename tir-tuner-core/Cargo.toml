[package]
name = "tir-tuner-core"
version = "0.1.0"
edition = "2021"
description = "Deterministic time-in-range glucose controller core with a Kani formal verification suite"
license = "MIT OR Apache-2.0"

[lib]
name = "tir_tuner_core"

[dev-dependencies]
proptest = "1"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(kani)'] }